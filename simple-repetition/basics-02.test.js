describe('Basics 02', () => {

    it('should understand capturing groups 01', () => { 
        /*
            Lets say we want to search for all name.png files, but we only want to return the name
            We can use a capturing group to capture just the name
        */

        
    });

});
